- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.antigen: antigen
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
